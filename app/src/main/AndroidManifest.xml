<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zaita.aliyounes.gsbvc2017">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/title_activity_login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.AjouteClientActivity"
            android:label="@string/ajouter_client_titre"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.ajoute_branch"
            android:label="@string/ajouter_branch_titre"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ajouter_supplier"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ajouter_brand"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ajouter_product"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ajouter_supplier_brand"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>